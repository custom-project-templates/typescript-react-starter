import React, { FunctionComponent } from 'react';
import Index from './pages';

const App: FunctionComponent<IProps> = ({}) => {
  return (
    <>
      <Index />
    </>
  );
};

interface IProps {}

export default App;
